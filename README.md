# PHP-Resize-Image

this function is meant to resize an image uploaded by an html form as needed (make it a square, a rectangle, a vertical image or simply resize it keeping the aspect ratio).

all the new sizes of the image (saved inside the $newSize array) can be edited as needed
